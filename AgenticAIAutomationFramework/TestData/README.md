# Test Data Directory

Store static test data files (JSON/CSV/YAML) here. Use environment-specific subfolders (dev/qa/prod). AI tools may consume these files for generating data-driven tests.

Example:
- TestData/dev/users.json
- TestData/qa/payment_credentials.yaml

